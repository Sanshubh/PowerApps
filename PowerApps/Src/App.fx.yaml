App As appinfo:
    BackEnabled: =false
    OnStart: =Set(varshoform,false);Set(varCurrentfrp,1);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

