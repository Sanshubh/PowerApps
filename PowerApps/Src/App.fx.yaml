App As appinfo:
    BackEnabled: =false
    OnStart: =Set(varshoform,false);Set(varCurrentfrp,1);Set(varCurrentDate,Date(Year(Today()),Month(Today()),1));
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

